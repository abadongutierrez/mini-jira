spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/minijira_db_dev
    username: test
    password: test
  jpa:
    show-sql: true

logging:
  level:
      com:
        jabaddon: DEBUG
      org:
        hibernate:
          SQL: DEBUG
          type:
            descriptor:
              sql:
                BasicBinder: TRACE